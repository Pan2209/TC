'use strict';

function multiplicarPorTres(arr) {
    for (let i = 0; i < arr.length; i++) {
      alert(arr[i] * 3);
    }
  }
  
  // Ejemplo de uso:
  let miArray = [1, 2, 3, 4, 5];
  multiplicarPorTres(miArray);